# Set these in the process environment or Render dashboard. This file is not auto-loaded.
TEMPLATE_PROVIDER=mock
TEMPLATE_MODEL=qwen-plus
FREE_DEMO_MODE=false
DASHSCOPE_API_KEY=
# Required on Render; username is reviewer. Use >=16 random characters.
APP_ACCESS_PASSWORD=
# On free Render: FREE_DEMO_MODE=true, TEMPLATE_PROVIDER=mock; do not configure a model key.
# HOST=0.0.0.0
